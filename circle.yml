machine:
  servcies:
    - docker

dependencies:
   override:
      - sudo pip install --upgrade docker-compose==1.5.0


test:
  override:
    - docker-compose run -d --no-deps web
    - cd app; mocha
